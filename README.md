# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные
В приложении используются две основные сущности данных: товар и покупатель.

### Интерфейс товара
Интерфейс описывает структуру данных товара, который отображается  каталоге интернет-магазина. 

interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

### Интерфейс покупателя
Интерфейс описывает структуру данных покупателя, которые вводятся пользователем при оформлении заказа.

interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}

## Модели данных
Для работы с данными, в приложении спользуются три модели: каталога товаров, корзины и покупателя. 

### CatalogModel
Класс CatalogModel отвечает за хранение и управление данными каталога товаров.
Модель используется для получения списка товаров, поиска товара по идентификатору и хранения выбранного товара для отображения в модальном окне.

#### Конструктор
Конструктор создаёт экземпляр модели каталога и инициализирует внутренние структуры хранения данных.

#### Поля
`products: IProduct[]`
массив всех товаров, полученных с сервера.

#### Методы
`setProducts(products: IProduct[]): void`
сохраняет массив товаров, полученный с сервера.

`getProducts(): IProduct[]`
возвращает массив товаров из модели.

`getProductById(id: string): IProduct | null`
возвращает товар по его идентификатору.

`setSelectedProduct(id: string): void`
сохраняет идентификатор выбранного товара.

`getSelectedProduct(): IProduct | null`
возвращает выбранный товар.


### CartModel
Класс CartModel отвечает за хранение товаров, добавленных пользователем в корзину.
Модель позволяет добавлять и удалять товары, получать список товаров в корзине и рассчитывать общую стоимость заказа.

#### Конструктор
Конструктор класса создаёт пустую корзину.

#### Поля
`items: IProduct[]`
массив товаров, добавленных пользователем в корзину.

#### Методы
`getProducts(): IProduct[]`
возвращает список товаров, находящихся в корзине.

`addProduct(product: IProduct): void`
добавляет товар в корзину.

`removeProduct(product: IProduct): void`
удаляет товар из корзины.

`clear(): void`
очищает корзину.

`getTotalPrice(): number`
возвращает общую стоимость товаров в корзине.

`getCount(): number`
возвращает количество товаров в корзине.

`hasProduct(id: string): boolean`
проверяет наличие товара в корзине по его идентификатору.


### BuyerModel
Класс BuyerModel отвечает за хранение данных покупателя, которые пользователь вводит при оформлении заказа.

#### Конструктор
Конструктор класса создаёт экземпляр модели покупателя с пустыми значениями полей.

#### Поля
`payment: TPayment | null`
выбранный способ оплаты.

`address: string`
адрес доставки заказа.

`email: string`
электронная почта покупателя.

`phone: string`
номер телефона покупателя.

#### Методы
`setData(data: Partial<IBuyer>): void`
сохраняет данные покупателя. Метод позволяет обновлять только часть данных, не перезаписывая остальные значения.

`getData(): IBuyer`
возвращает все данные покупателя.

`clear(): void`
очищает данные покупателя.

`validate(): Record<string, string>`
проверяет корректность введённых данных.
Метод возвращает объект с ошибками валидации. Если поле заполнено корректно, соответствующее свойство в объекте отсутствует.


## Слой коммуникации
В приложении реализован класс ServerClass, который отвечает за взаимодействие с сервером. 
Он использует композицию: внутри хранится экземпляр класса Api и через него выполняются запросы к серверу.

### ServerClass
Класс `ServerClass` выполняет две основные задачи:

1. Получение массива товаров с сервера (`GET /product/`) и передача их в модель каталога.
2. Отправка данных о заказе на сервер (`POST /order/`) и получение подтверждения успешной покупки.

#### Конструктор
Создает экземпляр, через который будут выполняться запросы к серверу

#### Поля
`api: Api`
экземпляр класса Api

#### Методы
`fetchProducts(): Promise<{ items: IProduct[] }>`
Выполняет GET-запрос к /product/ и возвращает объект с массивом товаров.

`sendOrder(orderData: IOrderRequest): Promise<IOrderResponse>`
Выполняет POST-запрос к /order/, передаёт данные о покупателе и товарах, возвращает объект подтверждения покупки.


## Представление

### Класс Header
Класс отображает шапку сайта и иконку корзины с счетчиком добавленных товаров.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)` 
Принимает объект для генерации событий, через который сообщается о действиях Презентеру, и ссылку на DOM элемент шапки.

#### Поля
`basketButton: HTMLButtonElement`
Элемент кнопки корзины на главной странице.

`counterElement: HTMLElement`
Элемент счетчика корзины.

#### Методы
`set counter(value: number): void` 
Обновление счетчика корзины.


### Класс Gallery 
Класс отображает каталог товаров.

#### Конструктор
`constructor(container: HTMLElement)` 
Принимает ссылку на DOM элемент, где будет отрисован каталог.

#### Методы
`setItems(elements: HTMLElement[]): void`
Отображение каталога товаров. 

`render(): HTMLElement`
Отображение контейнера.


### Класс Card
Базовый класс карточки, отвечающий за отображение полей карточек.

#### Конструктор
`constructor(container: HTMLElement)`
Принимает ссылку на DOM элемент и инициализирует компонент.

#### Поля
`titleElement: HTMLElement`
Элемент названия товара в карточке.

`priceElement: HTMLElement`
Элемент цены товара.

#### Методы
`set title(value: string): void`
Устанавливает название товара.

`set price(value: number | null): void`
Устанавливает цену товара. 

`set id(value: string)`
Устанавливает идентификатор товара.


### Класс CardCatalog
Отрисовка карточки на главной странице, наследуется от класса Cards.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`  
Принимает объект событий для генерации событий и DOM-элемент карточки.

#### Поля
`imageElement: HTMLImageElement`
Элемент изображения товара.

`categoryElement: HTMLElement`
Элемент категории товара.

#### Методы
`set image(value: string): void`
Устанавливает изображение товара.

`set category(value: string): void`
Устанавливает категорию товара и применяет соответствующий CSS-класс.

`handleClick(): void`
Генерирует событие открытия карточки (`card:open`).


### Класс CardFull
Отрисовка полной информации о карточке.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и DOM элемент карточки.

#### Поля
`imageElement: HTMLImageElement`
Элемент изображения товара.

`categoryElement: HTMLElement`
Элемент категории товара.

`descriptionElement: HTMLElement`
Элемент описания товара.

`cardButtonElement: HTMLButtonElement`
Элемент кнопки добавления товара в корзину или удаления из корзины.

#### Методы
`set image(value: string): void`
Устанавливает изображение товара.

`set category(value: string): void`
Устанавливает категорию товара и CSS-класс.

`set description(value: string): void`
Устанавливает описание товара.

`setButtonState(inCart: boolean, hasPrice: boolean): void`
Обновляет состояние кнопки в зависимости от наличия товара в корзине и наличия цены.

`handleButtonClick(): void`
Генерирует событие добавления или удаления из корзины в зависимости от наличия товара в коризне (`card:add`, `card:remove`).


### Класс CardBasket
Отрисовка карточки в корзине.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и DOM элемент карточки.

#### Поля
`indexElement: HTMLElement`
Элемент порядкового номера товара в корзине.

`deleteButtonElement: HTMLButtonElement`
Элемент кнопки удаления из корзины.

#### Методы
`set index(value: number): void`
Устанавливает порядковый номер товара.

`removeCard(): void`
Генерация события удаления товара из корзины (`card:remove`).


### Класс Form
Общий родитель двух видов форм. Содержит общую логику отправки формы, управления кнопкой и отображения ошибок.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации события отправки данных и DOM элемент формы.

#### Поля
`submitButton: HTMLButtonElement`
Элемент кнопки отправки данных.

`errorsElement: HTMLElement`
Элемент отображения ошибок валидации формы.

#### Методы
`submit(): void`
Генерирует событие отправки данных формы.

`setButtonState(enabled: boolean): void`
Активация или деактивация кнопки отправки в зависимости от данных в форме.

`showErrors(errors: Record<string, string>)`
Отображает ошибки валидации формы.


### Класс OrderForm
Дочерний класс Form, отображающий поля для выбора способа оплаты и заполнения адреса.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и ссылку на DOM элемент формы заказа.

#### Поля
`paymentButtons: HTMLButtonElement[]`
Элементы кнопок выбора оплаты.

`addressElement: HTMLInputElement`
Элемент ввода адреса.

#### Методы
`set payment(method: TPayment): void`
Устанавливает выбранный способ оплаты и обновляет состояние кнопок.

`set address(value: string): void`
Устанавливает значение поля адреса.

`selectPayment(method: TPayment): void`
Обрабатывает выбор способа оплаты и генерирует событие `order:change`.

`handleInput(): void`
Генерирует событие изменения адреса (`order:change`).

`submit(): void`
Генерирует событие перехода ко второй форме (`order:submit`).


### Класс ContactsForm
Дочерний класс Form, отображающий поля для заполнения электронной почты и номера телефона покупателя.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и ссылку на DOM элемент формы заказа.

#### Поля
`emailElement: HTMLInputElement`
Элемент ввода электронной почты.

`phoneElement: HTMLInputElement`
Элемент ввода номера телефона.

#### Методы
`set email(value: string): void`
Устанавливает значение поля email.

`set phone(value: string): void`
Устанавливает значение поля телефона.

`handleInput(): void`
Генерирует событие изменения данных (`contacts:change`).

`submit(): void`
Генерирует событие завершения оформления заказа (`contacts:submit`).


### Класс Modal
Класс модального окна, где будут отображены превью карточки, корзина, формы и сообщение об успешном заказе.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и ссылку на DOM элемент модального окна.

#### Поля
`contentElement: HTMLElement`
Контейнер для отображения содержимого модального окна.

`closeButton: HTMLButtonElement`
Кнопка закрытия модального окна.

#### Методы
`open(): void`
Открывает модальное окно, добавляя модификатор `modal_active`.

`close(): void`
Закрывает модальное окно, удаляя модификатор `modal_active` и очищая содержимое модального окна.

`handleClose(): void`
Генерирует событие закрытия модального окна (`modal:close`).

`setContent(content: HTMLElement): void`
Размещает контент в модальном окне.

`render(content: HTMLElement): HTMLElement`
Отображает переданный DOM-элемент внутри контейнера содержимого модального окна.


### Класс Basket
Класс корзины, отображающий товары, сумму товаров и кнопку.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и ссылку на DOM элемент корзины.

#### Поля
`itemList: HTMLElement`
Элемент списка товаров в корзине.

`orderButton: HTMLButtonElement`
Элемент кнопки для заказа товаров.

`totalPrice: HTMLElement`
Элемент отображения общей стоимости товаров.

#### Методы
`setTotalPrice(value: number): void`
Отображает общую стоимость товаров в корзине.

`submitOrder(): void`
Генерирует событие перехода к оформлению заказа.

`setButtonState(enabled: boolean): void`
Активация или деактивация кнопки оформления в зависимости от наличия товаров в корзине.

`setItems(items: HTMLElement[]): void`
Отображает товары в корзине.

`render(): HTMLElement`
Отображает список карточек товаров в корзине.


### Класс Success
Класс, отображающий сообщение об успешной покупке.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`
Принимает объект для генерации событий и ссылку на DOM элемент компонента.

#### Поля
`descriptionElement: HTMLElement`
Элемент отображения информации о списанной сумме.

`moreButtonElement: HTMLButtonElement`
Элемент кнопки возврата к покупкам.

#### Методы
`set total(value: number): void`
Устанавливает текст с информацией о списанной сумме.

`continueShopping(): void`
Генерирует событие закрытия модального окна (`modal:close`) после успешного оформления заказа.


## Презентер
В приложении используется архитектурный паттерн MVP (Model–View–Presenter).

Презентер реализован в файле `main.ts` и не выделен в отдельный класс. Презентер не хранит состояние и не содержит бизнес-логики. Его основная задача — обрабатывать события.

### Работа с моделями данных
Презентер обрабатывает события, генерируемые моделями:

`catalog:changed` — отрисовывает список товаров в каталоге (`renderCatalog`),
`preview:changed` — отображает полную карточку товара (`renderPreviewCard`),
`cart:changed` — обновляет счётчик корзины (`updateHeaderCounter`),
`buyer:changed` — обновляет состояние форм и ошибки валидации (`updateOrderForm`, `updateContactsForm`)

### Работа с представлением
Презентер обрабатывает пользовательские действия:

`card:open` — выбор карточки товара (`openCard`),
`card:add` — добавление товара в корзину (`addToCart`),
`card:remove` — удаление товара из корзины (`removeFromCart`),
`basket:open` — открытие корзины (`openBasket`),
`order:open` — переход к оформлению заказа (`openOrderForm`),
`order:change`, `contacts:change` – обновляет данные в модели покупателя, выполняет валидацию, обновляет ошибки и состояние кнопок (`buyer.setData`, `updateOrderForm`, `updateContactsForm`),
`order:submit` — переход ко второй форме (контакты) (`openContactsForm`),
`contacts:submit` — отправка заказа на сервер (`submitOrder`),
`modal:close` — закрытие модального окна (`modal.close`)

### Работа с сервером
Презентер взаимодействует с API: 

загружает список товаров при инициализации приложения (`loadProducts`), 
отправляет данные заказа на сервер и обрабатывает успешный ответ и отображает сообщение об успешной покупке (`submitOrder`, `Success`).


## События приложения

### События моделей данных
`catalog:changed`
Генерируется для загрузке списка товаров.

`preview:changed`
Генерируется для отображения полной карточки товара в модальном окне.

`cart:changed`
Генерируется при изменении содержимого корзины. Используется для обновления счётчика в шапке.

`buyer:changed`
Генерируется при изменении данных покупателя. Используется для обновления форм и валидации данных

### События презентера
`card:open`
Генерируется при клике на карточку в каталоге. Используется для открытия превью.

`card:add`
Генерируется при нажатии кнопки покупки товара. Используется для добавления товара в корзину.

`card:remove`
Генерируется при удалении товара из корзины или нажатии кнопки “Удалить” в карточке. Используется для удаления товара из корзины.

`basket:open`
Генерируется при нажатии на иконку корзины в шапке. Используется для открытия корзины.

`order:open`
Генерируется при нажатии кнопки “Оформить заказ” в корзине. Используется для открытия формы заказа.

`order:change`
Генерируется при выборе способа оплаты и вводе адреса. Используется для сохранения данных покупателя.

`order:submit`
Генерируется при отправке первой формы. Используется для перехода ко второй форме.

`contacts:change`
Генерируется при изменении email и телефона. Используется для обновления модели покупателя.

`contacts:submit`
Генерируется при отправке формы контактов. Используется для отправки заказа на сервер.

`modal:close`
Генерируется при нажатии на крестик или клике вне модального окна. Используется для закрытия модального окна.
